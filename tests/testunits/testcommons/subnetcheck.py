"""
Tries to check two ip addresses and check that they are on the same subnet.
"""

# ape modules
from apetools.baseclass import BaseClass


class SubnetCheck(BaseClass):
    """
    A class to check if two ip addresses are on the same subnet.
    """
