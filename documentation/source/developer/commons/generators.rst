The Generators
==============

These are just generator functions to be used to find files.

Find
----

The find-generator finds all file names that match a file glob and yields them one at a time.

Concatenate
-----------

The Concatenate Generator finds all the files that match a glob and yields all their lines.

