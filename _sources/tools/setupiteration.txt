Setup Iteration
===============

A module to hold a setup class for a single iteration. This looks like it was leftover from the code that evolved into apetools, as the `SetupeIteration` assumes that the device it holds is an Android.



.. uml::

   BaseClass <|-- SetupIteration

.. module:: apetools.tools.setupiteration
.. autosummary::
   :toctree: api

   SetupIteration
   SetupIteration.dumpsys
   SetupIteration.enable_wifi
   SetupIteration.disable_wifi
   SetupIteration.sleep
   SetupIteration.run
   SetupIteration.log

