Test Runner
===========

The `Test-Runner` is an attempt at a swiss-army knife style of test-runner. It is what `APETools` was meant to become but for now is called `ThroughputOverTime` because that's the current focus.

