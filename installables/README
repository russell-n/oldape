Installables
============

These are files that might be installed on a device to simplify interacting with it.

* wifi.sh : a DirectTV shell-script to use as a base-model
* wlanapi: a windows-based egg (installs as the `wifi` command)